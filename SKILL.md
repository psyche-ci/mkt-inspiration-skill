---
name: marketing-inspiration-pack
description: 营销灵感包：按热点趋势、AI新闻日报、营销案例三个固定栏目抓取、核验、摘要、排序和沉淀营销灵感，并支持读创意、收藏灵感和查灵感。Use for daily marketing inspiration refreshes, fixed-source advertising research, creative analysis, inspiration saving, and library search.
---

# 营销灵感包

## 随 Skill 提供的网页应用

本 Skill 安装包内包含 `app/` 完整 MKT INSPIRATION HTML 与素材，以及 `start-mkt-inspiration.mjs` 启动脚本和本地 AI 代理。调用需要交互网页时，必须先启动该脚本，再打开它输出的 `http://127.0.0.1:<端口>/mkt-inspiration-grid-preview.html` 地址；用户不需要另行安装第二个应用包。首次使用由用户自行设置 API Key。

### 唯一官方页面模板（必须遵守）

- 首页唯一入口和唯一视觉模板是 `mkt-inspiration-grid-preview.html`：保留原始 MKT INSPIRATION 顶部、三列×三行九宫格卡片、原始图片比例、边框、间距和翻面交互。
- 不得创建、复制、恢复或打开纵向列表式旧模板（包括 `AD INSPIRATION PACK`、`DAILY / LIBRARY / AI READING` 导航或单列案例列表）。不得用部署小工具的简化列表页替代九宫格首页。
- `start-mkt-inspiration.mjs` 的根路径必须返回上述九宫格首页；营销灵感库 `ad-daily-digest-library-preview.html` 仅作为收藏后的分类页，不得作为首页模板。
- 无论用户抓取的是“热点趋势”“AI新闻日报”还是“营销案例”，都必须在同一份九宫格首页中切换/更新对应一级分类，并始终保留页眉中的“营销灵感包”入口；不得为单一栏目另起旧版日报页、来源覆盖页或无导航页面。
- 工作区中历史遗留的 `ad-daily-digest-*.html`、`ai-news-daily-*.html`、`ad-daily-digest-home-preview.html` 及所有 `.backup-*.html` 均不得读取、复用、覆盖、打开或交付；它们不是数据源，也不是本次抓取缓存。本次交付只认 Skill 根目录 `app/public/mkt-inspiration-grid-preview.html` 及同端口的灵感库页。
- 发布或打包前，检查产物中不存在旧模板文件、旧模板入口或指向旧模板的重定向；只保留九宫格首页及其必需的灵感库页面和资源。

### 网页应用启动规则（必须遵守）

- 不得从 Finder、Skill 文件列表或聊天附件直接打开 HTML，也不得使用 `file://` 地址；页面之间的相对链接和图片资源依赖 HTTP 服务。
- 启动方式：在本 Skill 目录运行 `node start-mkt-inspiration.mjs`，以终端打印的 HTTP 地址为准；默认入口是 `http://127.0.0.1:8788/mkt-inspiration-grid-preview.html`，营销灵感包入口是同端口下的 `ad-daily-digest-library-preview.html`。
- 点击“营销灵感包”必须从上述 HTTP 首页进入，或在同一 HTTP 端口直接打开灵感库入口；保持启动脚本运行期间再进行抓取、读创意、收藏和灵感库操作。
- 若浏览器出现 `ERR_FILE_NOT_FOUND`，应判定为打开了失效的本地文件路径：重新启动脚本并改用其打印的 HTTP 地址，不要继续返回旧的本地文件链接。若出现 `ERR_CONNECTION_REFUSED`，先检查服务是否仍在运行并重启后再验收。

这是一个面向广告、品牌、内容和增长团队的营销灵感包：每天从三个固定栏目召回当天可核验内容，整理为可浏览、可筛选、可收藏的灵感卡片，并支持后续“读创意”和长期灵感沉淀。默认使用用户所在地时区；未提供时使用当前系统时区。

### Codex 调用后的强制引导与交付

- Skill 被安装或首次调用时，必须先向用户显示三条使用引导：输入“抓取今日营销案例 / AI新闻日报 / 热点趋势”抓取对应栏目；输入“抓取近期热门”抓取近期热门；不注明栏目会同时抓取三个栏目且耗时较长。
- 同一段引导必须说明：点击“AI帮你读”或“自定义上传创意”前，需要用户在网页中设置自己的 DeepSeek API Key；Key 仅保存在当前浏览器本地，Skill 不提供也不代存 Key。
- 还必须说明：喜欢的卡片点击“收藏”，可选择节点向、内容向、形式向或视觉向，保存到个人灵感包。
- 收到抓取指令后，不能只在聊天中返回文字列表。必须写入随 Skill 提供的 `app/public/mkt-inspiration-grid-preview.html` 九宫格页面（或同版式的当天 HTML），启动/复用本地 HTTP 服务，并在真实浏览器中打开 `http://127.0.0.1:<端口>/mkt-inspiration-grid-preview.html`；聊天回复只给收录摘要、覆盖状态和网页链接。
- 抓取完成后必须把浏览器页面置于九宫格首页，并验收目标日期、卡片数量、分类、封面和“营销灵感包”入口；未完成浏览器打开和验收，不得报告抓取完成。若浏览器当前停留在 `file://`、GitHub `blob` 源码页或旧端口，必须切换到本次 HTTP 地址。
- 若用户请求“打开营销灵感包”，从已打开的九宫格首页点击“营销灵感包”进入同端口 `ad-daily-digest-library-preview.html`，不得返回源码链接或仅描述页面内容。
- 安装后定位 Skill 根目录时，以当前加载的 `SKILL.md` 所在目录为准，再解析其 `app/public` 和 `start-mkt-inspiration.mjs`；不得猜测或硬编码 `/app/.agents/skills` 等不存在的目录。若这两个必需路径不存在，必须明确报告“Skill 安装不完整”，不得伪造已打开页面。
- 生成当天页面时，文件必须写入 Skill 根目录的 `app/public/`（或直接更新其中的九宫格首页），确保启动脚本实际能提供该文件；不得只把 HTML、图片放在临时输出目录后让用户手动拼装。
- 工具调用达到上限、单个来源失败或图片需要补核验，都不是跳过网页交付的理由；先完成服务启动、浏览器打开和真实状态提示，再报告未完成的来源或补核验项。

### 首次使用引导（必须显示）

- 输入“抓取今日营销案例”“抓取今日 AI 新闻日报”或“抓取今日热点趋势”时，只抓取对应栏目当天内容。
- 输入“抓取近期热门”时，抓取近期热门内容；未标注栏目时，三个栏目一起抓取，并明确提示耗时较长。
- 使用“AI 帮你读”或“自定义上传创意”前，先提示用户在浏览器本地设置 DeepSeek API Key；未设置时不得生成 AI 解读或自定义卡片。
- 用户看到喜欢的创意时，可点击“收藏”，选择节点向、内容向、形式向或视觉向加入自己的灵感包。
- Skill 安装包只提供空白页面、规则、脚本和素材；任何用户收藏、自定义上传和个人笔记只能保存在该用户浏览器的本地存储，不得写入 Skill、仓库或默认 HTML 数据。

三个首页栏目固定为：`热点趋势`、`AI新闻日报`、`营销案例`。`节点向`、`内容向`、`形式向`、`视觉向`仅用于灵感库收藏和聚合，不是首页一级栏目。

## 工作流

### 致命失败防线：今日要闻必须全来源、全板块

### 用户确认的固定抓取规则（不得自行改写）

- 三个首页栏目及固定网站：
  - `热点趋势`：TikTok Creative Center、Pinterest Trends、Product Hunt、Meta Newsroom · Instagram、Later · Instagram Reels Trends。
  - `AI新闻日报`：TechCrunch AI、The Verge AI、Hacker News、AIBase、AI Valley。
  - `营销案例`：数英网「项目」、SocialBeta「Campaign」、Adweek Creativity、The Drum Marketing、Event Marketer Latest。
- 广告案例固定入口：数英网 `https://www.digitaling.com/projects`；SocialBeta `https://socialbeta.com/campaign`；Adweek `https://www.adweek.com/creativity//?category=`；The Drum `https://www.thedrum.com/marketing`；Event Marketer `https://www.eventmarketer.com/latest/`。其他栏目必须进入其真实 Latest / Trends / News 页面，不使用搜索结果页代替。
- 广告创意案例来源固定为五个：数英网、SocialBeta、Adweek Creativity、The Drum Marketing、Event Marketer Latest。不得加入或恢复 LBB 及其他广告来源。
- 每次抓取先打开五个来源的指定列表页，只读取列表中的日期、标题、详情链接、封面和公开阅读量/点击量/观看数；只对目标日候选打开详情页补充正文和原图。
- 最新日期核验规则：每个来源都必须先进入其真实的 `Latest` / `最新` / `今日新闻` / `最新收录` 栏目，按列表顺序查看最新几条的日期与摘要，确认目标日后才打开详情；不需要读取整站、整页历史内容或所有图片。数英网必须进入“项目”栏目并点击最新项目卡片，逐条核对项目详情页日期后再返回，不能只看首页推荐或搜索结果。
- Latest 快速退出与超时规则：进入每个网站后，先在 `Latest` / `最新` / `今日新闻` / `最新收录` 列表中只检查最新条目的日期、标题和摘要；确认属于目标日才打开详情页。若列表中没有目标日内容，立即记录“已检查但无当日更新”并退出该网站，进入下一个来源，不继续浏览历史内容。单次抓取从开始计时起超过 5 分钟仍没有任何一条通过日期核验的新闻，必须立即报错并返回失败原因与已检查来源状态，不得继续等待、用旧缓存补齐或伪造结果。
- 今日要闻日期边界：只抓取当前目标日发布的内容；今日实际核验到几条就显示几条，最多 15 条。严禁返回前一日内容、旧缓存或历史卡片，也不得为了凑足 15 条回看前一天或更早日期。
- 浏览器核验是强制步骤：每次抓取必须在浏览器中逐一打开当前分类的每一个固定来源链接，读取其真实的 Latest / 最新 / News / Projects / Campaign / Work 页面；不得只打开其中一个网站、不得用搜索摘要、旧缓存、历史 JSON 或已有卡片代替本次页面核验。每个来源都必须留下“已检查并收录 / 已检查但无当日内容 / 页面异常待补核验”的状态。
- HTML 更新后的真实页面验收是强制步骤：完成抓取并写入 HTML、发布或替换线上版本后，必须打开实际浏览器页面（不是只检查静态 HTML、JSON、构建目录或源代码），确认页面已加载新目标日期、最新标题/卡片数量和对应分类内容；如果浏览器仍显示旧日期、旧卡片、空白页或无内容，必须继续修复、重新发布并再次打开核验，未通过真实浏览器核验不得向用户报告完成。
- 封面渲染验收是强制步骤：在真实浏览器中逐卡检查本次返回的每张卡片，确认封面元素已加载为实际图片或视频首帧，而不是灰色占位、破图、空白、文字占位或“原文未提供可核验封面”。正文有图时必须使用已核验的原图；远程图片加载失败时必须切换到已核验的本地封面或视频首帧。只看到日期、标题或图片 URL 存在，不算封面验收通过。
- 黑屏/无封面补救是强制步骤：任何列表页或详情页初始没有可用封面、显示黑屏/灰屏/破图、或卡片使用了文字占位时，必须先打开该条目的原文详情链接，在真实浏览器中查看正文首图、主视觉、`og:image`/`twitter:image`、图片画廊第一张或视频可见封面/首帧，并把实际可访问的图片地址回填到卡片后重新渲染。不能因为列表页没有封面就直接返回黑屏，也不能用“原文未提供可核验封面”替代这一步；只有详情页和正文确实没有任何可核验图像，且已完成浏览器检查，才允许标记为无封面并记录原因。
- 黑屏自查必须落到实际页面：任何由模板、SVG、CSS、文字或“数英网·日期”等生成的黑底证据图都视为未通过封面核验，不得作为最终封面返回。必须在浏览器点开该条目的原文详情页，查看真实首图/主视觉/图片画廊/视频首帧，提取并回填实际图片；回填后再次刷新浏览器逐卡确认图片已显示。只有原文详情页实际打开后确认没有任何图片或视频，才可显示明确的“原文详情页未找到可核验封面”，不得显示黑屏图。
- 每个一级分类下有多个固定来源时，必须全部打开并检查一遍后才可返回该分类结果。某个来源没有结果也要记录无结果，不能因为前一个来源已有内容就提前结束。
- 结果数量自检是强制步骤：如果某一分类最终只有 1 条或明显少于固定来源数量，必须重新检查该分类的全部来源列表、日期筛选和分页/加载状态，确认没有漏抓后才可返回；未完成自查时不得把单条结果当作完成结果返回。
- 三个一级栏目（热点趋势、AI新闻日报、营销案例）的展示顺序统一按公开阅读量、点击量、观看数排序；优先级为阅读量→点击量→观看数，指标相同再按点赞/评论/收藏/榜单，最后按发布日期。没有公开指标的条目保留，但必须标注“指标未公开/编辑精选”，不得猜数字。
- 营销案例每日展示上限为 15 条：五个来源合并后按公开阅读量、点击量、观看数排序，最多展示 15 条；不足 15 条按实际数量返回，不补抓其他来源。
- Event Marketer 不设置单站数量上限，参加五站合并后的 15 条排序。
- 任一来源或栏目无法访问，必须返回真实覆盖状态，不能用空白页或占位条目掩盖。

#### 当前用户固定来源（最高优先级）

- `热点趋势` 只检查：TikTok Creative Center、Pinterest Trends、Product Hunt、Meta Newsroom · Instagram、Later · Instagram Reels Trends。
- `AI新闻日报` 只检查：TechCrunch AI、The Verge AI、Hacker News、AIBase、AI Valley。
- `营销案例` 只检查：数英网、SocialBeta、Adweek、The Drum Marketing、Event Marketer。LBB、Muse by Clios、Marketing Dive、TOPYS 与 Ads of the World 均不抓取。
- `Adweek` 的固定入口为 `https://www.adweek.com/creativity//?category=`。
- `The Drum Marketing` 的固定入口为 `https://www.thedrum.com/marketing`。
- `Event Marketer` 的固定入口为 `https://www.eventmarketer.com/latest/`，只读取 `Latest` 列表；先按日期召回目标日作品，再按公开阅读量、点击量、观看数综合排序。营销案例每日最多展示 15 条；候选不足 15 条全部保留，超过 15 条只保留排序前 15 条，并记录候选数、入选数和指标缺口。
- 除非用户再次明确增补来源，否则不得自行恢复、扩展或搜索上述名单以外的网站。`references/sources-and-ranking.md` 中的旧扩展清单仅作历史参考，不参与当前抓取。

- 指令范围规则：`更新今日要闻/近期热门`表示同时更新今日要闻与近期热门两种视图；`更新营销案例今日要闻`表示只更新营销案例的今日要闻，不自动更新其它栏目或近期热门；只有明确写出`更新三类今日要闻和近期热门`，才抓取三个一级栏目（热点趋势、AI新闻日报、营销案例）的今日要闻与近期热门全部来源。若用户明确指定其它单一栏目，则只更新该栏目对应视图。
- 必须逐一打开上方当前固定来源及其 `Latest / 最新 / News / Projects / Campaign / Work` 栏目，只检查目标日。发现一条内容后不得提前停止，必须完成该分类全部来源的浏览器核验。
- 抓取必须采用两段式召回。第一遍只读取每个来源列表页中可见的发布日期、标题、详情链接、列表封面和榜单/互动指标，用日期筛出目标日条目；禁止在日期筛选前批量读取全部正文和全部图片。
- 第二遍只打开第一遍确认属于目标日的详情页，补充摘要、原文封面与必要事实。非目标日条目不得继续读取正文和图片。
- “最新几条”不是凭标题猜测：必须看到页面显示的日期（或明确的 today/latest 标记）并记录来源栏目。列表日期未确认前不得进入详情正文；确认是目标日后只读取该条摘要、封面和必要正文。若最新一条不是目标日，继续向下检查，直到确认目标日条目或明确到达日期边界。
- “尽快返回”不是固定两分钟硬截断。必须优先并尽快完成当前栏目全部固定来源列表召回；来源列表状态齐全后，仅对目标日条目做必要详情补充。详情页、图片下载或单站异常不得无限拖延整体结果；慢站应记录为“访问较慢/待补核验”，先返回已确认条目与完整来源覆盖状态。
- 为每个来源保存覆盖状态：`已收录及数量`、`已检查但无当日更新`、`重复或不合格`、`无法访问`。只有所有来源都有状态、单条结果已完成漏抓自查，才允许向用户报告“更新完成”。
- 任一板块为空时，不得直接显示空页面或声称抓取完成；必须先确认该板块的全部指定来源均已检查，并在页面上显示真实覆盖说明。
- 页面默认视图必须进入本次抓取实际有内容的一级分类，并默认选中该分类的“今日要闻”；不得让用户打开网页时落在无内容分类并看到“当前视图暂无已核验内容”。用户切换到无当日内容的分类时，应自动回到本次有内容的分类或显示该分类的真实覆盖说明，不展示孤立空白页。
- 漏查来源、漏更新板块、找到一条就提前结束，均视为致命失败；必须继续补抓与修正，不能用评分门槛、时间限制或单站访问失败替代全量来源检查。
- “近期热门”不是历史内容回填：必须进入指定网站的 Recent / Trending / Most Liked / Most Popular / 榜单等真实热门入口，读取页面公开的阅读量、点击量、观看数、点赞、收藏、评论或榜单排序，并按点赞量→收藏量→评论量从高到低呈现；指标相同再按网站榜单顺序。时间窗口固定为榜单抓取时点往前 14 个自然日，超过两周的条目即使仍在榜单上也不得抓取或展示；没有公开指标时保留网站榜单顺序并标注“指标未公开”，不得猜测数值。除非用户明确要求，不能用今日要闻内容代替近期热门。
- 营销案例近期热门只抓两个来源：数英网与 SocialBeta。数英网固定入口为 `https://www.digitaling.com/projects/hot`（项目 → 精选项目 → 近期热门项目）；SocialBeta 固定入口为案例热榜 `https://socialbeta.com/hot/case`。两站均只读取榜单列表中的标题、日期、详情链接、封面和公开热度指标，确认目标时间范围后再进入详情页。Adweek、The Drum、Event Marketer 仅参与营销案例今日要闻，不参与近期热门。
- 公开热门榜抓取通道：对数英与 SocialBeta 的公开列表页，优先使用带正常网络访问的直接 HTTP 抓取读取 HTML、内嵌 JSON 和列表链接；浏览器自动化只作为需要执行页面交互或补充详情时的第二通道。若浏览器页面超时但直接 HTTP 返回 200，不得判定为网站无法访问，应继续解析公开 HTML 并记录“浏览器超时、HTTP 已核验”。
- 用户写“更新今日要闻/近期热门”时更新两种视图；用户写“更新营销案例今日要闻”时只更新该栏目今日要闻；只有用户写“更新三类今日要闻和近期热门”时才更新三个栏目两种视图。

1. 读取 [references/sources-and-ranking.md](references/sources-and-ranking.md)，执行其中的重点来源与必查栏目清单，再确定候选、时间窗与排序。
2. 必须联网检索。优先使用批量搜索，再打开候选原文核验标题、发布日期、发布主体、内容与原始链接。对于依赖页面交互或登录态的来源，可使用浏览器工具；不要因个别站点不可访问而中止整份日报。
3. 只收录目标自然日内的合格内容；今日实际有几条就展示几条，最多 15 条。不回补前一自然日，不抓取上周内容，也不生成占位卡。不得为了达到数量上限误收旧内容、错误日期或未经日期核验的条目；每个一级分类独立按每页最多 9 条分页。
4. 收录具体广告作品、品牌 campaign、营销事件、创意项目和行业重大动态。合并同一事件的重复报道，以最接近原始发布方或信息最完整的页面作为主链接；不同作品、不同项目或同一品牌的不同 campaign 不得因配额被删除。
5. 为每条内容生成 50–100 个中文字符的原创摘要，回答“谁做了什么、创意/策略亮点是什么、为何值得关注”。避免复制导语、夸大效果或补写来源未证实的数据。
6. 为每条内容分配一级 `content_type`：`热点趋势`、`AI新闻日报`、`营销案例`。同时保留媒介/执行 `category`：`Video`、`事件营销`、`线下活动营销`、`新品上新营销`、`品牌联名`、`跨界营销`、`创意与设计`、`行业动态`等；`category` 不再作为首页一级筛选。必要时添加 2–4 个短关键词标签。
7. 三个一级分类（热点趋势、AI新闻日报、营销案例）的前后内容呈现顺序，都必须按公开阅读量、点击量、观看数排序；有多项指标时按阅读量→点击量→观看数的优先级比较，指标相同再比较点赞、好评、收藏、评论与站内榜单，最后按创意价值和时效性稳定排序。数据不可得时保留条目并标注“指标未公开/编辑精选”，不得伪造热度或评分。
8. 每天推送最新内容不超过 15 条：先按公开阅读量、点击量、观看数排序；如果当天总共抓取不到 15 条，则全部纳入；如果超过 15 条，则只选取排序前 15 条。该上限适用于每日推送的最新内容，排序只决定展示前后，不得为了凑数删除不足 15条的合格内容。
8. 为每条内容抓取并仔细检查原文可核验封面图，优先使用原文 `og:image` / `twitter:image`、正文首张案例相关图片、视频可见封面图或原始发布页主视觉。TVC/视频必须截取视频封面或首帧；正文有图时卡片必须显示对应封面。返回前逐卡检查实际渲染，禁止出现空白、白屏、黑色文字占位或加载失败图；若远程图失败，先改用已核验的本地封面/视频首帧，仍无封面则不得返回该条。
9. 输出 HTML 文件时，以随包提供的 [`app/public/mkt-inspiration-grid-preview.html`](../app/public/mkt-inspiration-grid-preview.html) 九宫格页面为唯一视觉与交互基准，不能改用旧的单列报告模板。读取 [references/html-spec.md](references/html-spec.md) 补充字段和验收要求；生成文件保存到当前工作区，文件名使用 `mkt-inspiration-YYYY-MM-DD.html`。页面应保持 MKT INSPIRATION 九宫格结构，并支持今日抓取、AI 读创意、收藏和灵感库交互。
10. 完成前检查：所有卡片链接可点击；封面图来源可追溯或明确显示无可核验封面；标题、来源、日期与原文一致；无重复事件；摘要长度合规；分类可筛选；AI 读创意默认隐藏且点击后有展开动效；移动端无横向溢出；页面中不得出现模板占位符。
11. 返回 HTML 文件的可点击绝对路径，并用一句话说明收录数量、时间窗及是否存在无法核验的来源。除非用户另有要求，不要在聊天中重复整份日报。

## 读创意与收藏灵感

当用户发送“读创意”或“收藏灵感”，并提供链接、正文粘贴、截图 OCR 文本或 brief 摘要时，读取 [references/creative-reading.md](references/creative-reading.md)，执行固定版式的 AI 读创意。

- 若用户提供链接，先尝试打开并核验原文；支持数英、SocialBeta、Ads of the World、品牌官网、公众号文章、Instagram、YouTube、B站、小红书等公开页面。
- 若链接需要登录、环境验证、地区限制、反爬或无法确认正文，不要猜测；明确标注“需要登录/无法核验”，并询问或接受用户粘贴正文。
- 若用户直接粘贴文字，将粘贴内容视为用户提供的待分析材料；不要声称已核验原始来源。若缺少品牌、日期、链接或媒介信息，在输出中标注“未提供”。
- 输出必须使用固定版式：创意洞察、创意内容、创意形式、可复用方法、适用节点、内容方向、形式方向、视觉方向、标签。
- 当用户说“收藏”“保存进灵感库”或“加入灵感包”时，把分析结果整理为可入库结构；优先保存到当前工作区的 `ad-inspiration-library.json`，并在可行时同步更新 `ad-inspiration-library.html`。
- 灵感库不是单纯上传页。它必须同时接收两类内容：每日抓取页中被收藏的广告案例，以及用户通过链接、粘贴正文、截图 OCR 文本手动上传的创意。用户点击收藏时，应让用户选择收藏夹：节点向、内容向、形式向或视觉向。
- 自定义上传创意必须提供独立入口：用户粘贴网址、正文、推文、截图 OCR 文本或 brief 后，自动生成一页与收藏卡片一致的版式。卡片固定包含标题、原文/来源、封面（有可核验图片时使用原图）、创意洞察、创意内容、创意形式、个人笔记、查看原文和关联笔记；不得只保存一段原文或跳转到空白上传页。网址无法访问时保留链接并明确标注“原文待核验”，不得编造正文事实；用户只提供文字时，以文字为唯一分析依据并标注“用户提供内容”。创意形式必须是具体执行形态（如病毒广告、TVC、线下快闪、户外广告、平面海报、社交互动、品牌联名、包装设计、事件营销），禁止使用“品牌内容”“营销活动”等泛化词。
- AI 解读支持用户自带 API Key：当前只保留已核对的 DeepSeek 供应商。用户首次设置后应持久保存在当前浏览器本地安全存储，后续打开、刷新或切换页面无需重复输入；允许用户主动更换 Key，系统不得主动清除已保存 Key。界面只显示“API Key 已设置”或脱敏后的末四位，绝不显示完整 Key；完整 Key 仅用于本地代理调用，不得写入 HTML、抓取数据、Skill、安装包、日志或示例。分享 Skill 时必须让每位使用者单独输入自己的 Key。
- 供应商兼容性边界必须明确：DeepSeek 使用 OpenAI-compatible `POST /chat/completions`，Base URL 为 `https://api.deepseek.com`，默认模型为 `deepseek-v4-flash`。这不是“任意字符串 API Key 通用”；未核对的其他供应商暂不提供。每次保存或首次 AI 解读前必须实际发送一次最小测试请求并显示成功/失败原因；失败时不得伪造 AI 结果，应提示 Key 无效、模型不存在、余额/权限不足、地域不匹配、网络不可达或接口不兼容等具体原因。
- AI 解读创意时，必须把对应详情页 URL 抓取到的标题、正文、图片上下文作为唯一事实依据，分别生成创意洞察、创意内容、具体创意形式；禁止随机生成、脱离链接补写、把导航关键词当正文，或在无法读取正文时假装完成。AI 调用失败、正文为空或字段缺失时必须停止生成并提示错误。
- AI 帮你读不得只回填预置短摘要：点击后必须把对应详情页 URL 交给本地 AI 代理，由代理先读取可访问的原文标题和正文，再连同已核验的封面/视频上下文发送给用户自己的模型。创意洞察、创意内容要具体并完整保留，翻面卡片支持滚动查看；创意形式只输出 2–5 个具体执行关键词；API 调用失败、原文无法读取或正文不足时必须显示真实错误/待核验状态，不得静默回退成模板短句。
- AI 帮你读的详细度规则：必须实际调用用户已保存的 API Key 和所选模型，不得用本地预置文案冒充 AI 结果。`创意洞察`和`创意内容`各自用 2–3 句完整句子，必须完整保留并可在翻面卡片内滚动查看，不得用 CSS 行数截断、自动省略号或隐藏后半段；讲清用户问题、核心策略、具体执行和品牌/产品如何进入。不能压缩成一句话、关键词堆砌或泛泛写“提升品牌曝光”。`创意形式`根据原文判断具体执行形态，只返回如“事件营销、户外广告”“TVC、平面海报”这类具体关键词，不写长段解释，也不得使用“品牌内容”“营销活动”等套话。模型未返回有效结果时必须显示真实错误并要求重试，不得回退到短摘要。
- AI 解读结果的卡片呈现规则：必须实际调用用户 API Key；`创意洞察`、`创意内容`各用 2–3 句具体、连贯的话讲清核心用户问题、策略和执行，必须完整保留，禁止 CSS 行数截断或自动省略号；翻面卡片可滚动查看全文。`创意形式`只返回 2–5 个与原文对应的具体执行关键词（如“事件营销、户外广告”“TVC、平面海报”“线下快闪、社交互动”），不得写成长段解释，也不得使用“品牌内容”“营销活动”等泛化套话。若模型完全没有可解析结果，必须显示真实错误；部分字段缺失时保留已返回字段并标注“原文未说明”。

## 查灵感

当用户发送“查灵感 + 关键词”时，只检索当前灵感库 `ad-inspiration-library.json`、当前浏览器本地收藏和用户本次提供的内容；禁止读取任何历史 `ad-daily-digest-*.html`、`ai-news-daily-*.html`、备份页或旧缓存。若本地没有灵感库，说明当前尚未沉淀长期库，并建议先用“收藏灵感”或每日抓取建立数据。

## 搜索与核验要求

- 数英网必须打开当日「项目」最新列表，并点击最新几条项目进入详情页核验日期、摘要和封面；不能只读取列表卡或首页。SocialBeta 必须检查当天案例流。英文广告来源仅检查 Adweek Creativity、The Drum Marketing、Event Marketer Latest。LBB、Muse by Clios、Marketing Dive、TOPYS 与 Ads of the World 均不抓取。
- 其他固定来源同样必须按其页面实际栏目核验：先找并打开该站的 Latest / 最新 / 今日新闻 / 最新收录入口，再按日期与摘要筛选；禁止因为某站没有明显“Latest”字样就跳过，必须记录实际使用的最新栏目或页面状态。
- Instagram 动态仅通过 Meta Newsroom · Instagram 与 Later · Instagram Reels Trends 两个固定入口获取，不直接扩展到 Instagram 账号池。
- 以原文页显示的发布时间为准；搜索结果摘要只可用于发现候选，不可单独作为收录依据。
- 遇到只有日期、没有时刻的文章时，将其视为当日发布并标注“日期级核验”。日期无法确认、不是目标日、或仅能从旧缓存/搜索摘要推断的内容，一律不得进入主榜。
- 无法打开原文或确认发布时间的候选项不得进入主榜，可放入页面末尾“待核验线索”，并明确说明原因。
- 不设置同一品牌或同一来源的数量上限。完整性优先；仅合并同一作品或同一事件的重复报道，并保留不同项目、不同 campaign 与不同创意执行。

## 页面内容

- 页眉标题必须为 `营销灵感包-YYYY年M月D日`；页面、文件名说明和浏览器标题均不得写成“广告灵感包”。
- 页眉显示生成时间、统计时间窗、收录数量和一句不超过 36 字的今日观察。
- 页面包含两个主要视图：`今日抓取` 和 `灵感库`。`今日抓取` 顶部固定提供三个一级分类按钮：`热点趋势`、`AI新闻日报`、`营销案例`；点击后只显示该分类内容并独立分页，不按来源比例混排。
- 今日抓取打开时，默认定位到本次实际有内容的一级分类及其“今日要闻”分页；若用户请求的是“营销案例”且该分类有结果，必须直接打开“营销案例 / 今日要闻”，不能落在其他分类或无内容分页。
- 今日抓取的每条新闻必须采用九宫格卡片结构：三列×三行等宽卡片，包含编号/日期、原文封面图、标题与标签、摘要、热度依据、查看原文、AI帮你读和收藏入口。AI帮你读内容默认不展示，必须在用户点击按钮后在当前卡片翻面显示；每页最多 9 张卡片并支持分页。
- 灵感库视图必须包含上传链接输入框、粘贴文字/推文/图片描述输入区，并展示来自“每日抓取收藏”和“自定义上传”的内容。
- 灵感库视图必须按四类组织：节点向、内容向、形式向、视觉向。节点向用于圣诞节、春节、情人节等节点；内容向用于情绪、内容主题、文化洞察和可复用创意方法；形式向用于海报、包装、快闪、短片、UGC 挑战等媒介和执行形式；视觉向用于色彩、画风、构图、字体、插画、摄影、包装视觉、海报风格等视觉表达。
- 页面末尾包含逐来源覆盖说明与方法说明，区分“已检索且无更新”“已收录”“重复或不符合范围”“本次无法访问”；不要暗示已完整抓取无法访问的网站。
- 不使用外部 JavaScript、字体或 CSS 依赖；所有样式和筛选脚本内嵌，确保本地直接打开可用。

## 触发行为

收到“更新今日要闻/近期热门”时立即执行完整工作流，同时更新当天三个固定栏目的今日要闻与各网站近期热门；收到“更新营销案例今日要闻”时，只更新`营销案例`的今日要闻；只有收到“更新三类今日要闻和近期热门”时，才对三个固定栏目完整执行两种视图的全来源抓取。所有指令均按本 Skill 的全来源核验、真实覆盖状态与最多 15 条规则生成“营销灵感包” HTML；旧触发词“广告灵感”仍可作为兼容说法，但不得作为页面或产品名称。若用户指定日期，则使用该日期结束时刻向前 24 小时的窗口，并在页眉说明。
